# CodePilot 项目宪法

**版本**：V1.0 · **适用范围**：项目三 MVP 及其后续兼容版本

这份宪法是 CodePilot 的最高工程约束。需求、代码、配置、评测和部署发生冲突时，先遵守本文件；需要改变本文件时，必须留下架构决策记录、迁移方案和对照评测结果。

## 第一条：项目目标

CodePilot 必须交付一个可复现、可审计、可恢复的 Python 代码审查闭环：输入合成 Diff/ZIP，输出结构化审查意见、候选补丁、Docker 验证证据和人工审批结果。项目三的核心成果是受控的 A2A Agent 协作，不是 Agent 数量或模型参数数量。

## 第二条：运行模式

系统必须支持 `single`、`a2a`、`offline` 三种模式。

- `single` 是基线和故障降级模式；
- `a2a` 是项目三的主模式；
- `offline` 只运行确定性规则和 AST，不能生成未经验证的补丁。

三种模式必须复用相同的 Finding、ImpactReport、PatchCandidate、VerifyEvidence、状态机、权限策略和安全门禁。

## 第三条：唯一事实来源

PostgreSQL 是任务、状态、子任务、Artifact、审批、幂等记录和审计事件的事实来源。状态机 checkpoint 只保存恢复意图，
不得成为绕过数据库约束的第二状态库。缓存丢失不能改变业务结论。

## 第四条：任务与状态

每个请求必须有父任务；每个 Agent 调用必须有 `task_id`、`parent_task_id`、`trace_id`、`correlation_id`、`protocol_version`、`deadline` 和 `idempotency_key`。Coordinator 是父任务唯一 owner；Agent 只能更新自己的子任务。

父任务合法主路径为：

```text
DRAFT → REVIEWING → REVIEWED → FIXING → TESTING → PENDING_APPROVAL → MERGED
```

任何失败、超时、越权、Schema 错误或无法确认的未知状态都必须进入 `NEEDS_HUMAN`、`FAILED` 或 `REJECTED`。禁止从终态回退，禁止跳过审批进入 `MERGED`。

## 第五条：A2A 契约

Agent 必须先通过版本化 Agent Card 注册，再接受 Task。跨 Agent 数据必须封装为 Message 和 ArtifactEnvelope，并通过 JSON Schema、内容哈希、父子关系和协议版本校验。

MVP 只保证受控的 HTTP/SSE A2A 子集。公网发现、跨组织信任、动态热更新和生产级服务治理不属于 MVP，不得作为主流程隐式依赖。

## 第六条：权限与副作用

默认拒绝所有能力和工具。Review、Impact 不得写文件；Fix 只能生成候选补丁；Verify 只能验证；只有 Coordinator 在质量门禁和审批通过后才能调用任务分支写入。

任何 Agent、重试逻辑或 A2A 消息都不得直接执行合并、修改 `main/develop`、改变审批结果或绕过 Docker 沙箱。所有副作用必须有状态校验、幂等键和追加式审计记录。

## 第七条：恢复与幂等

遇到超时、网络断开或进程重启，必须先查询任务状态和历史幂等记录，再决定是否重试。未知状态禁止盲目重放写操作。相同幂等键必须返回原结果，不得创建第二个执行实例或第二份补丁副作用。

## 第八条：安全底线

提交代码只能在 Docker 沙箱中运行，沙箱必须无网络、非 root、只读根文件系统、限制 CPU/内存/磁盘和执行时间，并清理容器与临时目录。输出进入日志或 API 前必须脱敏。以下安全不变量必须为零：越权写入、未审批合并、重复副作用、沙箱逃逸和非法状态迁移。

## 第九条：确定性优先

规则引擎、AST、调用图、scope drift、补丁格式、质量门禁和状态迁移属于确定性边界，LLM 不得改变其结论。LLM 只负责受约束的解释、建议和候选内容生成；结构化输出失败必须拒绝或转人工。

## 第十条：评测与发布

每次发布必须使用 20 个黄金 PR，并分别运行 single、a2a、offline。报告至少包含审查召回率/精确率、补丁成功率、pass@3、pass^3、A2A 路由正确率、Artifact Schema 通过率、恢复成功率、Trace 完整率、Token、延迟和全部安全不变量。

a2a 只有在质量不低于 single、安全不变量不退化，且在质量、恢复或可观测性至少一项有数据提升时，才能宣称带来收益。延迟增加超过 30% 必须在报告中解释。

## 第十一条：范围与变更

项目三不做真实 Git 平台接入、远程 PR 推送、多语言、Kubernetes、多租户、自动绕过审批的合并和模型微调。
允许提供本地 Git 只读适配器，把 `base..head` 转成服务端既有的 Diff 输入；它不得读取远程凭据、修改调用方工作目录或绕过审批。
新增需求必须标记 P0/P1；任何 P1 不能破坏 P0 契约。

模型微调属于项目四候选路线。只有在基线误差已定位、数据完成脱敏和隔离、存在未微调对照，并通过代码可应用性、测试、安全和成本评测后，才可接入模型层。

## 第十二条：完成定义

功能只有同时满足以下条件才算完成：

1. 有对应需求编号、Schema、状态迁移和错误码；
2. single 与 a2a 通过同一契约测试；
3. 正常、超时、重复、断线、重启和越权场景可复现；
4. 结果可按 trace 从父任务回放到 Agent、工具、Artifact 和审批；
5. 测试通过且安全不变量为零；
6. README、配置示例和迁移说明已同步。

## 附录：冲突处理顺序

安全底线 → 状态与审批 → A2A Schema 与幂等 → 确定性质量门禁 → 业务功能 → LLM 体验 → 性能优化。任何“更智能”或“更快”的方案都不能降低前面层级的约束。
